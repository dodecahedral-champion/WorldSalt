graph G {
	MODEL [shape=tab];
	RENDER [shape=tab];
	THINK [shape=tab];
	SIMULATE [shape=tab];
	PERSIST [shape=tab];
	NETWORK [shape=tab];

	MODEL -- RENDER;
	MODEL -- THINK;
	MODEL -- SIMULATE;
	MODEL -- PERSIST;
	MODEL -- NETWORK;
	THINK -- NETWORK;
}

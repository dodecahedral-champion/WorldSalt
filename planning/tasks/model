return {
   {"model a block with no data"},
   {"model a grid without subgrids"},
   {"break up grid block storage into subgrids"},
   {"model an entity with position and attached grid"},
   {"add orientation to entity"},
   {"include grid bounds on entity"},
   {"include block mass"},
   {"compute entity total mass"},
   {"compute entity centre of mass"},
   {"add velocity and motion to entities"},
   {"add force vector to entities"},
   {"accelerate entities under force"},
   {"add spatial grids, defined by absolute position"},
   {"attach each entity to a spatial grid"},
}
